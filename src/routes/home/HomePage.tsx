import React, { PureComponent } from 'react';

import s from './HomePage.scss';

class HomePage extends PureComponent {
  render() {
    return (
      <div className={s.root}>HOME PAGE!</div>
    );
  }
}

export default HomePage;
